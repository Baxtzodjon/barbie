/* let num = 20.49
let numTwo = 22 */

/* let arr = ['abdukhafiz', 'alsafir', 'bobur', 'islom', 'aziza', 'azizabonu', 'xojik', 'baxtzod', 'abdumanon', 'safir', 'shakhzod', 'akbarshox']
arr[Math.floor(Math.random() * 13)] + " durak" */

let num = 20.699213123

console.log(
    /* Math.ceil(num) */ /* округяет цифру в потолок */
    /* Math.floor(num) */ /* округяет цифру в пол */
    /* Math.round(num) */ /* округляет до ближайшего целово */

    /* Math.max(num, numTwo) */
    /* Math.min(num, numTwo) */

    /*  parseInt(num) */ /* переводит из сторки в число убирая запятые */
    /* parseFloat(num) */ /* переводит из сторки в число */
   
    /* num.toFixed(1) */
    

    /* git config --global user.name "Baxtzodjon"
    git config --global user.email "rasulovbaxtzod@gmail.com" */

    /* console.log(); */

);